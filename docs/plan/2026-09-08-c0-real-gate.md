# C0 real 调度与 #646 合并门

> 📋 方案待拍板 · 状态由 docs-autosync 自动登记，作者请按实修改

状态：实施中。用户授权仅测试脚手架与证据；不改生产 lane、回放引擎、基线或体验问题。

## 现状与范围

C0 的 --real 在启动前无条件 blocked-budget；dry-run 的供应商预设嵌在 UI 流程里。
这是尚未实现的测试能力，不是生产修复。将供应商准备/观察抽为调度器，UI 创建项目、
输入剧本、拆镜头、批准、生成、入轴、导出、冷重启仍共用一条流程。
真实设置仅经 isoApp.prepareIsolation 复制到隔离目录，应用 readCatalog → decryptApiKeyRecord 读取；
不读取项目库，不输出密钥，不向 fixture 写真实密钥。四路目录全隔离。

## 先查别人

- 复用 tests/ux/pd1-deferred-tools-cache.probe.mjs:70 的主进程凭据与余额读取，:304 的文件桥。
- 复用 evals/lib/isoApp.mjs:27 和 tests/ux/_launchApp.mjs；不另造 launcher。
- 复用现有生产映射与 provider 出站，不自建供应商 SDK。只在本次测试进程拦截 fetch 预算。
- 官方 https://apimart.ai/pricing （2026-09-08 实抓）H3 768P 折后 $0.05712/s；64s=$3.65568，按预算汇率7为 ¥25.58976；未折扣上界 ¥31.9872。
- 官方 https://docs.apimart.ai/en/api-reference/videos/minimax-h3/generation.md：768P、4–15秒、带音轨。
- 官方 https://docs.apimart.ai/en/api-reference/images/gpt-image-2/generation.md：异步生成、size比例、resolution档。
- 官方 https://docs.apimart.ai/en/api-reference/account/token-balance.md：token独立余额用于对账。
协议偏差：无，保持生产请求与响应；未知路由/模型/报价拒绝出站。

## 预算与验收

本轮用户明确授权总额 ¥8，覆盖旧 runbook 的 ¥5，但不自动改变 H3/64秒的内容约束。
开跑前实时获取公开报价，先检查完整片长估价；超限不能开始付费。每次请求先持久预留上界，
并发、失败和重试不退预留；仅数值与模型/路径/参数白名单可进证据。账单与估算分开。
R30 用真实事件中的首调与终态计数；0分母写 N/A。未跑到的截图/MP4不得制造。

验证：Node 预算边界回归 + 既有 fixture 回归；gates；任务 commit cherry-pick 至 e99c70aae；
先 build（含 Vite）再 dist:mac:dir；候选包 C0 --real、同提交 replay:shadow；逐张阅图（若到达），
保留失败与未执行项。#646 正文追加证据，不合并、不改 draft。回滚仅 revert 本任务测试/文档提交。

## 本地实现验证（2026-09-08）

- 完整 gates exit 0：75 contracts 零阻断（文档项 advisory）；Vitest 11981 passed/2 skipped；runtime306/306；janitor13/13、stats8/8；build通过。
- 针对性 Node 7/7：未知收费入口、上限、失败/在途预留、真实响应转发、R30与八镜媒体回归。
- 共用 UI dry-run 全链路通过：64秒，1920×1080，AAC；MP4 SHA256 `760d8f6370e3b032f445462135bcc48c2d63d5cee93331d2c49657fa821f6e1b`。
- `tests/ux/shots/g1-c0/attempt-pIXVrS/` 的13张图均逐张复核，记录4类摩擦（报价、缩略图、画布缩放/平移、审片JSON），未改生产代码。
- 真实报价预检实测 blocked-budget（全流程保守预留¥33.56955），设置复制=false，付费0。真实完整调度与成片仍未验证，不能称真实C0通过。
- L2 默认扫描真实项目与host对话；与本任务“不得读真实项目库”冲突，已请求仅L2只读范围确认；未授权前不运行默认扫描，不伪造200回合语料。

Ponytail 复核：删除重复64秒断言，共用流程先验片长再计算R30。保留必需的调度器 attach：
将它改成可选调用会让真实模式缺少预算初始化时静默继续，违反安全依赖不可optional的R28；
双调度器显式实现同一接口，非缺省回退。其余阶段空实现保留同一接口约定。

补充零额度装配验收：`node tests/ux/g1/c0-real-assembly.e2e.mjs` 已通过，实际Electron中的
readCatalog→decryptApiKeyRecord、文件桥、预算预留、响应转发与未知模型拒绝均被执行。
使用独立空设置与合成加密凭据，两种传输在装配前换成内存函数；真实付费调用0，不冒充供应商成功。

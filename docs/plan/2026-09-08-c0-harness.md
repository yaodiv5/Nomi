# C0 执行脚手架

> 📋 方案待拍板 · 状态由 docs-autosync 自动登记，作者请按实修改

状态：零额度开发构建执行链 VERIFIED；真实模型与候选打包版未验收。

## 范围和验收

`tests/ux/g1` 原创输入、00–07 走查、隔离证据；在现有 walkthrough 门岗扫描 g1 目录。只替换供应商 HTTP 为 loopback，文稿、分镜审批、物化、生成、时间轴和导出均由 UI 驱动。八镜各 8 秒；模拟片不是故事质量证明。

验收：CLI 非法组合拒绝；端点接收八个不同镜头；落盘分镜 64 秒；八个视频按顺序入轴；Nomi 导出可解码、16:9、60–120 秒且含音轨的 MP4；冷重启同一资产/片段仍在；每步稳定截图、时间、摩擦日志。未人眼复核不得写无摩擦或 C0 通过；R30 模拟与真实数字分开。失败非零退出，保留每次独立 attempt。

## 先查别人

- 仓库已有分镜提案和审批：`tests/ux/agent-runtime-production.walk.mjs:73`，复用 `nomi_canvas_plan` 和现有审批控件，不注入分镜终态。
- 仓库已有 SDK HTTP 夹具：`tests/ux/agent-runtime-fixture.mjs:144`，复用文本供应商；只增加逐镜视频 loopback 响应。
- 仓库已有生成视频入轴：`tests/ux/agent-runtime-video-export.walk.mjs:175`，复用用户可见的加入时间轴和导出入口。
- 仓库已有四路隔离：`tests/ux/_launchApp.mjs:185`；断言/截图用 `tests/ux/_assert.mjs:1`。不新造 launcher 或断言框架。
- 依赖现场：主会话已按 frozen lockfile 安装本 worktree 依赖，未新增依赖。此次沿用既有 Playwright 调用，没有引入 SDK、协议或通用框架；生态选型、TikHub 用户调研不适用于这次用例脚手架，不作已检索声明。

结论：复用现有测试体系；业务 fixture 只覆盖 C0 输入与逐镜模拟产物。

## 不动和回滚

不动两个 lane 目录、主 checkout、其他 worktree、真实项目库、依赖、lockfile 和 CI 付费路径。本轮不调用付费模型。

`--real` 当前仅预算阻断入口：没有核实全成本报价与在途封顶机制，返回 `blocked-budget`。它尚未实现读隔离设置 key、MiniMax H3 真实调度、真实账单和完整人工看片；这些不能列为已交付。价格未知时，在读任何真实凭据或发出请求前停止，遵守上游 C0 runbook。

`--packaged` 接受绝对 .app 或可执行文件路径，仍使用隔离 profile；打包版未验证。源码 SHA 与运行时版本分别记录，不能由源码 HEAD 推断外部包的构建提交。

回滚：删除本次新增文件及门岗新增扫描段；本次隔离证据保留在 shots 下，不改真实项目。

## 本轮验证和边界

- 开发构建 `pnpm run build` 通过；隔离启动、原创剧本落盘、分镜审批与八镜生成已实际运行。
- 最终 contracts：73 项，70 通过、0 阻断、3 advisory（索引/状态/研究来源），退出0，206.9秒；夹具 Node 测试、walkthrough 与 prior-art 复验通过。
- 模拟视频供应商必须声明媒体档案，否则 `availableModels.ts` 会过滤无档案的媒体模型；复用 `wan-2.7` 的档案结构、8 秒和 16:9 参数，只访问独立 loopback 供应商。自动审片也走同一文本夹具，返回无法判断的零分，不伪造故事质量结论。
- 失败 attempt 均保留：缺档案、漏自动审片期望、画布裁切，以及空格平移误激活仍聚焦的入轴按钮。脚手架使用中键平移，避免焦点控件的空格激活；每次入轴断言数量，最终核对节点引用与连续起止帧，重复素材不能假绿。
- `delivery:preflight` 已刷新，HEAD 与 origin/main 同为 `93c74930908743b6d8890bf7c8dc7ab42b84b4c5`；dirty 只来自上轮 C0 草稿，不清理覆盖。
- `--real` 仍是 blocked-budget，未实现真实调度；本轮只交付零额度执行证据，不能称真实 C0 完成。

## 开发构建干跑收据（2026-09-08）

`node tests/ux/g1/c0-short-film.walk.mjs --dry-run` 退出 0，00–07 通过；证据 `tests/ux/shots/g1-c0/attempt-IgCaEN/`。八镜各8秒，经真实UI入轴、预览、导出，冷重启节点result和剪辑保持一致；00–07共51.534秒，打断2次，付费调用0/¥0。模拟分镜首调1/1、回合1/1；自动审片8次返回无法评判，真实R30为0/0 N/A。

Nomi MP4：64秒，1920×1080，H.264+音轨，19,911,223字节，完整解码通过。SHA256 `887d58f967e171c59e91ae0d56d5ffb471b897b53653a26e466985bf4a62eddc`。Codex逐张读取00–07、审批/提交与首中尾共13图，逐步摩擦在 `tests/ux/shots/g1-c0/emotion-log.md`；不是独立真人完整看片。

视觉边界：分镜生成后短暂显示加载失败；初入画布/冷重启截图部分缩略图未加载；自动审片JSON刷入对话；逐镜入轴需要额外平移；生成确认没有人民币报价。这些明确保留为真实候选包体验验收项，本PR不改生产UI，不称真实C0或体验全通过。

交付前快进整合 `origin/main@87087c345`（新增仅方案/索引文档，无生产代码变化）；六个提交文件共513行，未改lane或依赖。

## CI 收货修红（2026-09-08）

症状：#645 Unit 在 c0-fixture.node-test.mjs 报 spawnSync ffmpeg ENOENT；Quality Gate 因该失败注解连带红。更新 main 后 run 34224120037 同样失败。本机 PATH 指向空目录复现，红证据 /tmp/nomi-c0-no-path-red.log。

直接原因：C0 生成、探测、导出解码按裸命令查系统 PATH。类根因：测试脚手架没有使用项目已声明、按平台解析的媒体二进制。分类 recurring：任一干净 CI 或无全局 FFmpeg 的机器都能复发。归属测试夹具/走查执行层；不改生产代码、依赖或 CI 配置。

先查别人：tests/ux/fixtures/canvas-performance-fixture.mjs:9、tests/production/real-draft-film.test.mjs:30 已使用 installer 的 path。已读本地 node_modules/@ffmpeg-installer/ffmpeg/index.js 与 @ffprobe-installer/ffprobe/index.js：都解析平台二进制绝对路径，缺失时报错。复用既有包公开 path，不造查找器、不增 PATH fallback；无需依赖升级或数据迁移。

范围：C0 fixture、Node 回归、walk 的所有 ffmpeg/ffprobe 调用。同类扫描 rg 覆盖 tests/ux/g1 三个入口；删除这些裸命令调用。回归将 PATH 设为空目录，仍须生成八段独立视频并逐一验证时长/画幅/音轨，断言不放宽。验收：Node 回归、相关 Vitest、contracts、gates 与 Linux CI；回滚仅还原这三个脚本及本段说明。原有真实模型/打包版验收边界不变。

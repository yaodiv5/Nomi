# PR #658 第二轮 CI 证据修复

> 📋 方案待拍板 · 状态由 docs-autosync 自动登记，作者请按实修改

状态：调查中。固定工作目录与既有分支，不新建 PR。

## 范围与验收

先复现 canvas full 2/2 与 real-user loopback，保留原断言并输出入口真实 DOM；以报告、截图和时序定根因。先红后绿日志位于 `docs/plan/process-feedback-evidence/ci-round2/`。完成后跑 smoke 与加锁完整 gates，正常 commit/push 既有 PR #658。

## 现状与边界

开工 HEAD f84bcf9d9。已有 PF-LAST.md 收据改动及未跟踪 liveness.json 原样保留，不混入本次修复提交。preflight 已刷新远端但因这两项既有改动报 dirty_worktree；手动 fetch 确认当前分支包含 origin/main。不修改断言凑绿、不跳过；reactFlow 只允许根因直接涉及行。回滚使用本次 scoped commit 的 revert，不重置工作树。

## 先查别人

- `tests/ux/_launchApp.mjs:239`：隔离 userData/settings/projects 由启动器单一创建，已有目录不清空；batch 每次独立 launch，不跨脚本复用窗口。
- `tests/ux/canvas-batch-production.walk.mjs:223`：#663 把首启 splash/tour 预置到 initialLocalStorage，没有共享前一场景 DOM；本次只补真实计数和失败截图。
- `electron/capabilityCore/mcpRpcError.ts:35`：#661 已在共享序列化器复用公开错误投影，修复 GUI RPC 与 stdio 的领域码丢失；本次只验证，不另写序列化器。
- `tests/ux/_canvasHit.mjs:42`：已有空白命中白名单；只有拿到点击误命中的证据才接入，不能凭失败标题替换手势。

已检查 #663（d566c99c3）的真实 diff：batch walk 删除首启 reload/跳过流程，改由 initialLocalStorage 预置 splash/tour；不跨脚本复用 Electron。真实旅程须先检查当前 main 是否已有修复，避免重复实现。新外部格式、框架、依赖均不在范围内。

## 待补证据

症状、直接原因、类根因、同类入口扫描与修复层待本地输出确定，不能由失败标题推断。

## 已验证的旅程根因

CI artifact `linux-walkthrough-evidence` 的 report 指向 production-mcp:205：新建项目没有创作文档时，应保留 document_not_found；旧 RPC 序列化只保留 RpcError，丢掉领域 Error 的公开码。此修复已由 #661 合入当前 HEAD，位置 electron/capabilityCore/mcpRpcError.ts，合同 docs/fixes/2026-09-09-mcp-rpc-public-error-parity.root-cause.json。不得重复造实现。

本地单变量证据：只把 dist-electron 的该条件恢复成旧行为，完整 loopback 5/7，production-mcp 同一断言 errorCode=null；当前 L2 也会检查该序列化不变量，所以同样红。随后完整恢复原构建文件，原断言 7/7，production-mcp 58 assertions。源码未改，费用零；contact sheet 已人眼检查。原始当前 HEAD 初跑也是 7/7，不能冒充红。

## 画布排除证据与 Linux 诊断

当前 HEAD full 2/2=7/7，timelineHandle count=1。4 倍 CPU、本地 1680×1020 渲染视口、同视口恢复 af088e848 的三个生成反馈组件，batch 原断言均通过，无法声称已本地复现。macOS 真实窗口曾被系统限制到 1680×844，视口覆盖实验仍通过。

下一步只提交诊断：保留原断言，在失败点打印 role 匹配、原始 DOM/隐藏祖先、展开态 section、对话框与视口；失败截图写到 CI 已上传的 outputs 目录。先拿 Linux 现场，禁止依据两种猜测改产品或隔离层。

## 后续 CI 与软件渲染实验

当前 f84bcf9d9 的 run 34286456362：真实旅程原失败已消失；batch 改在第 310 行确认生成后等待 scheduled navigations 超时；performance 在 waiting-effects 达到 20 分钟上限。它们不同于 af088e848 的时间轴断言，不能混为同一证据。

本地 SwiftShader：旧反馈组件 batch 通过，当前 img-fx 在通知动画截图前超时（canvas-software-current.log）。浏览器 failIfMajorPerformanceCaveat 在本机 SwiftShader 仍返回可用（webgl-caveat-probe.log），不能盲用该选项声称性能保护成立。这项尚待有边界的性能修复，不在本轮伪称完成。

已正常整合 origin/main ac9f5cf22（#662），merge commit e05a5e9c2；保留既有交接改动。新增点击目标日志只观察节点和时间轴，无行为修改。

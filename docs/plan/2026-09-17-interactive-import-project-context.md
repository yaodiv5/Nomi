# Interactive import project context

> 📋 方案待拍板 · 状态由 docs-autosync 自动登记，作者请按实修改

Approved PR 802 follow-up. Cross-pool review #2 confirmed capacity/image dimensions awaited before capturing a project; upload/recovery/video-duration completion can apply to the subsequently hydrated global store. Retry retained File but no originating identity. Prior art and product decisions remain in 2026-09-17-pr802-root-causes.md.

The existing project coordinator owns a project execution context: immutable ProjectBinding plus permanent cancellation signal/assertion, independent of page and port revision. Capture it before any asynchronous import preparation; invalidate on hydration replacement, release, or coordinator unregistration. Guard every node mutation and upload completion. Pass originating binding and assertion to the shared byte/native/remote importer, so main session publication guards also apply. Retry stores this original context and cannot revive after A→B→A. Cancellation and stale identity terminate as handled empty results (retry returns false); ordinary recoverable upload errors retain their existing behavior. Clipboard captures before conversion or remote fetch and never falls back after cancellation. The library calls the adapter synchronously instead of awaiting a dynamic import before identity capture.

Scope: coordinator, canvas local-file and clipboard adapters, library import entry, their tests, API transport regression tests, root-cause contracts. No new session owner, media policy, UI controls, or background-generation policy. Tests pause capacity, dimensions, upload, recovery, data-URL conversion, clipboard remote fetch and video-duration operations, then replace the project; B remains untouched and A→B→A cannot resume. Same-project page changes retain identity. Original byte/native import transport tests remain required. Remote storage guard is integrated by the root agent. Revert the scoped commit to roll back; no data migration.

## Final census follow-up

The importer/persistence machine census found 34 direct references. The original fix did not yet cover file drops onto existing nodes, parameter/reference slots, storyboard reference uploads, clip/composer/director uploads, and image transformations that persist before writing a node. These share the same missing lifetime across awaits. Capture the existing coordinator context at the start of each interactive action, pass it through persistNodeImageFile/Blob to the existing importer, and assert before node mutations, fallback and component callbacks. Never turn cancellation into null or a data URL. Normal upload failure retains the existing fallback. Delayed React state updaters also check the originating context when executed.

Persistence helper interface: persistNodeImageFile(file, nodeId, context) and persistNodeImageBlob(blob, nodeId, name, context); the context is required (superseded: the earlier immediate-capture default was deleted by the single issuance point below). Helpers never create a separate session. Transformations and file-reader preparation capture before their own first await, then explicitly pass the context. Root integration owns useNodeImageEditing/buildContactSheetNode/useNodePanoramaHandlers; the independent reviewer owns whiteboard and ScreenshotCropOverlay. This branch owns the helpers and manual upload entries.

Explicit importAudioToLibrary writes an explicitly named project's asset library without a canvas projection and retains project IO semantics. Agent delivery keeps its separately owned session/target guard. Artifact rasterization and the director AI scene save turned out to be user actions whose writes happen after awaits (the scene save even used the upload API's current-project fallback), so both now issue their project at the action start. New red tests cover cancellation during persistence and reference-slot callbacks; final integration checks the complete door table.

## Single issuance point (2026-09-17 structural convergence)

User ruling: key = trusted window + project; project IO authority is issued only at the action entry and discarded after. `captureCurrentProjectExecutionContext` and the coordinator capture method are now module-private; the only issuers are `withProjectAction` (renderer-started action, called before any await) and `withMainProjectAction` (an action main started itself and named by its exact committed binding). Every shared helper (persistNodeImageFile/Blob, importLocalMediaFilesToGenerationCanvas, pasteClipboardMediaToGenerationCanvas, importClipNodeAsset, useNodeImageUpload, rasterizeArtifactToReferenceAsset, extractShotCutsToNodes) requires the issued context; defaults and `??=` late fallbacks are deleted. Main side: the global screenshot hotkey fixes the canvas read surface registry epoch and `captureAssetWriteContext` before `desktopCapturer.getSources`, and the renderer-reported screenshot project id channel is deleted; frame extraction IPC joins the trusted session before any await (`createProjectInteractionCapture`, shared with imports) and `extractVideoFrameToAsset` fixes disk identity before ffmpeg. No product behavior change: same-project page changes keep the epoch; replacement (including A to B to A) cancels. Class test `src/workbench/project/projectActionIssuance.contract.test.ts` (AST scan for late issuance / optional context / private issuer reference, plus `@ts-expect-error` compile half under check:test-types).

Stopped short of id-level current-project reads (`getActiveWorkbenchProjectId` / `getDesktopActiveProjectId`, 97 sites) at that milestone; closed by the 1b section below.

## 1b: delete the current-project readers (2026-09-17)

Rulings: (1) interactive actions issue with `withProjectAction` and re-check after each await; a switch cancels and writes nothing back. (2) Background generation: the run's project identity is fixed at submission and persisted with the run; polling and recovery read only that identity; `taskApi` never overrides an explicit projectId and has no fallback; results land in the original project (store if open, else the existing per-project disk path); old records derive identity from persisted fields or fail with an explanation. (3) Other windows: no new owner; a child window derives from the parent window's trusted session, is revoked with it and is refused without one. (4) `assetUploadApi` binding is required.

Scope: `runProjectDelivery.ts` (outcome routing owner) and `nodeRunOutcome.ts` (single patch shape) for background runs; required `target` / `projectTarget` / `TaskProjectIdentity` through runner, catalog task, text stream, relay frame, video depth, shot verify and recovery; main task query without the committed-project fallback; `windowProjectCapture.ts` shared by the screenshot hotkey and browser/child-window IPC; `useOpenProjectId` for display-only reads; delete `src/desktop/activeProject.ts`, `getActiveWorkbenchProjectId`, `getCanvasEventsProjectId`, `activeTaskProjectFallback`. Not in scope: product behavior (§2 unchanged: same-project navigation keeps working, submitted runs are never cancelled by a switch), Electron walkthrough (second step), new storage. Rollback: revert the 1b commits; `GenerationNodeRunRecord.projectId` is optional, so records written meanwhile stay readable. Acceptance: red→green per category (logs in the review disposition), the reader ratchet in `projectActionIssuance.contract.test.ts`, contracts `2026-09-17-background-run-project-identity` and this plan's contract, typecheck/test-types/lint/contracts.

## 先查别人

- 依赖里已有：TypeScript DOM lib 提供 `AbortSignal.any` 组合取消（node_modules/typescript/lib/lib.dom.d.ts:2787），签发出的 context 用它把项目交互生命周期与 coordinator 注册生命周期合成一个 signal，不自造取消树。
- 依赖里已有：Electron `desktopCapturer.getSources` 只在主进程、返回 Promise（node_modules/electron/electron.d.ts:7642；官方文档 https://www.electronjs.org/docs/latest/api/desktop-capturer 同述）——抓屏必然跨 await，身份必须在调用前固定。
- 仓库里已有：主进程发布身份 owner `captureAssetWriteContext`（electron/assets/assetWriteContext.ts:18）与画布读面 registry 的已提交 epoch 捕获 `captureCommittedCanvasReadPort`（electron/capabilityCore/canvasReadSurfaceRegistry.ts:606）；截图与抽帧直接复用，不新建截图身份 owner。
- 仓库里已有：renderer coordinator 的项目交互生命周期（src/workbench/project/projectCanvasReadSurface.ts:262 `withProjectAction` 收口处），把原来散落各处的自取改成唯一签发。
- 生态里已有：VS Code 扩展 API 的 `CancellationToken` 由调用方在动作起点创建并作为参数传入每个异步 API（https://code.visualstudio.com/api/references/vscode-api#CancellationToken，如 `lm.invokeTool` 签名），被调方从不自取全局取消状态——与「handler 只接收不自取」同构。
- TikHub 自媒体：未查。本条是应用内部的项目生命周期/权限约束，没有面向终端用户的做法可比。
- 仓库里已有（1b）：按项目读写盘的 owner `readLocalProjectAsync` / `saveLocalProject`（src/workbench/library/localProjectStore.ts:132、134），关闭项目删结果已在用；后台结局落盘直接复用，不新建存储。
- 结论：用已有（coordinator 生命周期、主进程 registry epoch、AssetWriteContext、localProjectStore），只把「谁能签发」收成一处、把 Run 的项目身份固定在提交那一刻，并用编译器与类测试钉住；不引入新库、不新建身份 owner。

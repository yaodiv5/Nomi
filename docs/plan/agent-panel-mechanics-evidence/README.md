# B2a visual and regression evidence

> 📋 方案待拍板 · 状态由 docs-autosync 自动登记，作者请按实修改

Real components mounted through AgentPanelV4Panel, with deterministic recorded shapes: folded.png, context.png, model.png, queue.png, approval.png, preview.png, next-step.png, model-narrow.png. Each was visually inspected after the final composer spacing repair. browser-result.json records geometry, queue deletion, next-step exposure and preview subtree inert assertions.

Actual Electron: electron-restored.png and electron-context.png were captured from the final build after reopening the isolated project used for the two real DeepSeek tasks. The full model label and all footer controls fit the actual 340px panel. real-model-result.json preserves sanitized answers, receipts, usage and final geometry. electron-loopback-folded.png is the earlier zero-provider retry-storm walkthrough (three intentionally failed writes folded into one receipt).

Red regression outputs are retained as red-*.txt. context-red.png, preview-red.png and next-step-red.png accompany the original failures. red-narrow.txt records the additional actual-width failure; model-narrow.png shows its final passing state. The monochrome browser fixture is a component test host, not a proposed B2b design.

Supplier-native cost is unavailable in RuntimeUsage; existing costUsd remains labeled USD. The real provider returned token usage but no monetary amount. Media generation count: zero.

# skill-ui-b：同类变体收束与封面补齐

> 📋 方案待拍板 · 状态由 docs-autosync 自动登记，作者请按实修改

状态：已完成实现与验收；最终完整gates在aa94da24305b上exit 0，同分支PR #685交付。继承任务分支与 PR #685；preflight HEAD 205a0f595、origin/main ad95ab47e，干净。原未跟踪雷达文件保存在 /private/tmp/nomi-skill-ui-b-preserved/liveness.json，结束恢复。

## 范围与根因
表情拥挤来自 electron/promptLibrary/builtinExpressionPack.json 的 25 条预设（五种情绪各五档）。四个点名技能仅各有一个配方；未把它们正文中的表情拆为 UI 项。SkillLibraryContent、V4SkillPopover、PromptLibraryContent 都把同源条目平铺；NodeEffectChips 仅显示 curated effects 和用户提示词，不含这 25 条，但运镜等同类可同样膨胀。类根因是发现层没有共享的类别与数量收束规则，判 recurring。

## 方案与验收
沿用户任务书给定的「类别 · N 项 ▸」实施：共享分组投影以 curation.group 或明确的公开源 sourceId/source 分组；不根据标题猜表情、不把无类别用户条目强行合并。超过 4 项默认折叠，展开全部；卡片按组，列表按组，搜索结果保留精确计数与可展开入口。复用原生 details/summary、既有 WorkbenchMenu 与虚拟列表，不接新框架。C20 规则写入设计系统。
先用真实宿主截现状（/private/tmp/nomi-skill-ui-b-before），再保存与真实布局对应的 HTML 样张；结构遵从本任务已指定的折叠形态。夹具覆盖 12 项默认一行/展开12项、4/5阈值、另一类别与无类别输入。

## 封面事实与限制
实际已有55项媒体声明、40张效果插画（后续 gpt-image-2 收据补完37张）；v1报告过期。剩33个老技能无 library 声明。使用既有 metadata.nomi.library.preview，不新增 cover 平行字段，不改技能正文。已消除模型歧义：任务书没有指定Gemini，仅要求沿用同模型；最新已批规则与round3收据为gpt-image-2，所以直接按已批路径执行，前面确认问题撤回。预算≤¥12、总调用≤90，每条只准首轮+一次目检返工。逐张目检不过不用，88项接触表诚实记录。

## 先查别人 / 标准
- Agent Skills https://agentskills.io/specification：复用 metadata 扩展点；沿用现有嵌套 nomi 偏差（领域元数据），不新建格式。
- 本地既有实现 electron/shared/skillCuration.ts:9、src/design/menu.tsx:95、src/workbench/promptLibrary/PromptLibraryPanel.tsx:124 为接线依据；不改变模型可见工具或 Agent 行为。
- 封面官方契约：https://docs.apimart.ai/en/api-reference/images/gpt-image-2/generation.md；实查 image_urls 支持 data URL，size=16:9，resolution=1k；以最新已批准模型发起付费。
- 本仓 package.json:7 的 AGPL-3.0-only 与 https://spdx.org/licenses/AGPL-3.0-only.html 对齐；扩展原有许可枚举，不更换技能许可。

## 不动项与回滚
冻结 electron/agentLane、src/workbench/ai/lane、reactFlow；不改技能正文、不更新设计实验室基线、不绕过钩子。变更限发现UI/元数据/媒体/验证。按任务提交回滚；已消耗生成费用不可回滚。

## 交付门
红绿夹具→真实 Electron 折叠/展开/选择/卡片墙截图→全部 gates exit 0（等锁）→同分支 push 与 PR 正文追加→12行以内交接文件及指定 scratchpad 副本。禁止合并PR。

## 真机发现与收敛
88项文件完整性通过后，真机发现29个老技能仍因缺少既有 selectable-in-workbench 显式声明被隐藏。保留单一可选策略，不放宽路由资源过滤器；仅为本次策展的33份元数据声明展示。新增48技能展示夹具先红后绿，技能正文与整合后的HEAD逐字节相同。

## 冷启动验收补充
最终生产构建冷启动暴露内置表情被外部仓库下载阻塞。共享getPromptLibrary改为先返现有本地snapshot，原有单inflight缓存后台刷新；新外部内容下次reload可见。复用现有TTL/磁盘缓存/种子，不新增缓存或事件协议。无网络完成和旧磁盘缓存两类夹具先红后绿，再跑完整gates与最终真机。

# PR #658 CI 修复

> 📋 方案待拍板 · 状态由 docs-autosync 自动登记，作者请按实修改

状态：已验收（完整 gates exit 0）。

范围：当前任务分支，先修 WebGL 类型与非法正则，再采样版本切换与批量确认点击；不新建 PR、不改无关内核。

原始 CI：版本错误信息第二次读已是 v1 current；批量确认日志 click action done 后等待 navigation 超时，没有 intercepts 记录。不得预设浮层是根因。

## 先查别人

- 依赖里已有：`node_modules/typescript/lib/lib.dom.d.ts:13407` 的 webgl overload 返回 WebGLRenderingContext | null，直接使用标准重载，删除非标准 experimental-webgl 分支。
- 测试框架已有：`node_modules/playwright/types/test.d.ts:9123` 的 toHaveAttribute 提供状态断言与默认 expect timeout；使用已有重试，不增加时间或自造轮询。
- 仓库里已有：`src/workbench/generationCanvas/nodes/NodeResultStack.tsx:284` 的 switchTo 同时服务缩略图与设为当前按钮，updateNode 同步更新 store；`tests/ux/_assert.mjs:1` 提供项目统一 Playwright expect，直接复用。
- 生态/用户方案：此次是现有实现的编译错误与观测时序错误，没有选型或新能力；TypeScript/Playwright 自带声明与真实 store/DOM 采样足以裁决，不以社区搜索替代本地证据，也未声称进行 TikHub 调研。
- 结论：用已有。保留共享动效降级与框架断言，无新增运行时/调度器。

## 修复与验收

修复分类 recurring：软件 renderer、SSR、无 WebGL 均可重入共享动效判定；版本 DOM 异步提交可跨机器复现。先记录红片，再在实测对应层修。

验收：typecheck、指定 Vitest；SwiftShader 真实 Electron 两分片 7/7 + 6/6；完整 python3 scripts/with-gates-lock.py -- pnpm run gates exit 0（最长排锁 40 分钟），正常 hooks commit/push 现有分支，再写 PF-LAST 顶部收据。

回滚：仅 revert 本轮 scoped commit；已有未跟踪文件原样保留；PF-LAST/PF-RULINGS 不提交。

## 原失败 1：版本切换

症状：CI 即时读取失败后，第二次错误详情却已读到 v1 current。直接原因：走查未等待 React commit。类根因：把 store 的同步更新当作 DOM 的同步更新。

本地 SwiftShader 实采 card-sampling.log：store update 同一瞬间 store=image-v1、DOM=image-v3；随后 100ms ×10 均为 v1。card-red-observation.json 保留真实不同步窗口（不是伪造一次本地全走查失败；旧全走查本地这次通过）。修在测试观察层，使用 Playwright 原有 toHaveAttribute 重试，期望仍严格为 true，不增加超时。历史排序及重开持久化断言保留。

同类入口检查：NodeResultStack 的缩略图及「设为当前」都进入 switchTo→updateNode；图片/视频同一组件。单次读取不成立，存储更新无需修改。

## 原失败 2：批量确认

原日志 click action done 后等待 scheduled navigations 超时，无 intercepts pointer events。elementFromPoint 实采命中生成按钮；禁止据此去改 pointer-events 或放置层。GL_RENDERER 已确认真实 SwiftShader Device。通过受控构建变异撤掉软件渲染准入，复测批量生成，结果见 batch-red.log；恢复原构建后复测，见分片收据。

所有 reactFlow/** 本轮无修改。例行雷达沿用本 session 前一轮已记录结果，不重复扣额度或改基线。

反证结果：batch-red.log 在真实 SwiftShader 上准确复现同一按钮 click action done → waiting for scheduled navigations → Timeout 30000ms。唯一变异是关闭软件 renderer 降级，按钮命中无遮挡。直接原因是软件 WebGL 上动画负载令确认点击后的渲染响应超时；类根因是动效准入缺失 GPU 能力判定，修复归共享 useReducedProcessMotion。恢复的同构建 batch-renderer.log 完整 PASS；最终两分片再验，不改 click 或超时。

## 交付核对

- 初始 HEAD 183f77561ce378bc3f1deb06823a646830140deb；delivery:preflight 因任务开始就存在的两份未跟踪制品拒绝 clean 检查，未移动/丢弃它们。显式 fetch 后 origin/main=50f10ea9a，merge-base 等于 origin/main，无落后提交。按本任务指定保留当前 worktree/分支。
- typecheck exit 0；指定 pnpm run test -- generationFeedback useReducedProcessMotion exit 0（该脚本实际运行全量：12103 通过/2 跳过）；额外定向 Vitest 两套 10/10。
- 本轮 card-stack-green.png 已人眼检查：当前版本指示、托盘、图片/视频、控件可见；未改产品布局。
- 模型调用仅隔离本地 loopback，费用 0。

最终画布验收：full 1/2 6/6 exit 0、full 2/2 7/7 exit 0；收据在 ci-round4/full-{1of2,2of2}-summary.json 及同名 .log。

排锁期间 origin/main 前进到 0c11429573fd（#664），第一次 gates 被 fresh-base 拒绝，收据 gates.log exit 1。已正常 merge 到 206fec5d6190268d25435293f8e19e1f46bc982c，恢复本轮 scoped stash；未 push。新基线仅为测试观察器等变动，无本轮生产文件冲突；两分片与完整 gates 在合并后重跑。

合并后复验：merged-full-1of2.log 6/6 exit 0；merged-full-2of2.log 7/7 exit 0，summary 已更新为合并后收据。完整 gates 最终日志为 gates-final.log。

最终 gates：2026-09-09 08:15:19 完成，gates-final.log 的 GATES_EXIT_CODE=0。76 contracts：73 通过、0 阻断、3 advisory；全量 Vitest 12103 通过/2 跳过；其余测试链与 Vite/Electron 构建均通过。

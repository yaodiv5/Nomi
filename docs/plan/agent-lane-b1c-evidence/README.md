# B1c cache-prefix evidence

> 📋 方案待拍板 · 状态由 docs-autosync 自动登记，作者请按实修改

- `prefix-red.txt`: actual 24-turn loopback failed before the production change; tools/system changed after a group request.
- `prefix-green.txt`: 24 recorded user turns, 24 tool-group requests, 48 real HTTP requests; tools/system prefixes identical. Legacy unlocked and locked histories preserve their respective file access.
- `schema-before.txt` / `schema-after.txt`: `check:model-schema`, full resident catalog 12,121 → 9,893 pi-estimated schema tokens; 45 tools. Runtime and gate share description projection and resident-menu selection; schema constraints remain intact.
- `deepseek-sample.json`: final source, real DeepSeek v4 flash via configured APIMart endpoint; isolated document/timeline ports. Tools correct 4/4, user tasks 3/3, next request after switching has cacheRead=16,384 and input delta=75. Seven requests per sample; three development samples total ¥0.552033 (un-discounted configured pricing, USD→CNY ×7). No paid media generation.

The 10k figure measures tool descriptions plus JSON Schema using pi's estimator, not provider-total input. Full usage guidance/examples moved to the fixed system segment and still incur cold-input tokens. Raw provider usage in the sample is authoritative for this endpoint. Other endpoints are not claimed as paid-tested.

Full gates execution is serialized with the repository lock. Final exit/commit/push receipt is in SWITCH-LAST and the task's scratchpad copy; no new PR or package.

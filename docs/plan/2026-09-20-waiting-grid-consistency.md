# 生成等待层去除蓝色横带与四实例硬限制

> 📋 方案待拍板 · 状态由 docs-autosync 自动登记，作者请按实修改

状态：调查与实施中，尚未交付。用户 2026-09-20 明确选择「优先让所有可见卡片播放格子动画，先实测性能再确定限制」。

## 旧行为与根因

`GenerationWaitingSurface` 只向前四个 eligible 消费者分配动画槽位；其余以及低缩放、离屏、减弱动态和无 GPU 消费者渲染中间横带。9 月 9 日方案保留静态带，9 月 14 日将实心带改为扫光，并没有删除降级分支；不是图片比例或镜号修改重新引入。扫光 CSS 仅遵循系统媒体查询，因此显式 reduced/GPU 不可用时也可能继续动画。

缺失不变量：普通可见图像/视频等待应复用已批准格子效果，能力降级不应重新引入用户拒绝的中间蓝条；数量阈值必须有实际测量依据。

## 范围与复用

- 共享等待组件拥有准入和降级视觉；沿用 img-fx 的共享 renderer、帧率、暂停、主题、真图揭示与销毁，不新增动画引擎。
- 先独立测量 4/8/16 实例，再在真实 Electron 画布测量全部可见等待节点；根据证据取消或调整旧四实例上限。
- 保留视口、40% 缩放、软件渲染/无 GPU、系统减弱动态、隐藏页暂停以及最终图 1100ms 卸载期限。
- 不满足动画条件的图像/视频使用不运动的 token 格子纹理，不保留旧扫光。音频仅保留原有等高灰柱。
- 导入共用等待层，保留真实字节驱动渐显；不改底部真实百分比和生成状态词。
- 独立伴随修复：GPU 探测重复创建/未显式释放，见 `2026-09-20-process-motion-probe.md`。

## 先查别人

- 仓库内唯一共享入口：`src/workbench/generationCanvas/nodes/GenerationWaitingSurface.tsx:48`，机器数门两个消费者 `NodeGeneratingOverlay` 与 `NodeImportingOverlay`，无需再造一套。
- 已安装上游源码：`node_modules/img-fx/dist/index.es.js:1170` 每实例 shader 绘制后复制至 2D canvas；`:1209` 共用 renderer 循环；默认约 10fps。共享 renderer 并不等于多个实例免费，须实测。
- 已安装上游 API：`node_modules/img-fx/dist/index.d.ts:593` 与 `index.es.js:1219`：没有无 WebGL 的静态格子快照入口，paused 仍需 WebGL 初始化；降级采用纯静态 CSS 纹理，不 fork shader。
- 原方案：`docs/plan/2026-09-09-process-feedback-imgfx.md`，历史 120FPS 是四动画场景的 UI 帧率，不等于动画帧率，不能据此声称无限节点安全。

## 修改对应审计与验收

1. 删除槽位登记/订阅时，真实八节点测试必须先在旧实现读到四个而失败；修改后全部符合准入的节点有动画。
2. 移除 CSS 横带与 keyframe，同步性能诊断/走查锚点；降级正向断言格子存在、零动画、没有 WebGL，不以全部空白冒充成功。
3. 图片/视频/导入/音频分别验证，主题与 zh/en 截图亲眼检查。
4. 实测等待、拖动/缩放、离屏和批量完成卸载；沿用既有性能预算，不提高门槛。
5. 按风险运行 gates，独立评审、Ponytail、PR 与授权合并，真实 merge SHA 验证后才称已解决。

## 回滚

按本任务提交 revert；无项目数据迁移。保留真实百分比、结果 URL 与生成状态契约。

## 未证明边界

低配置物理设备、长时间电量和全硬件覆盖不能用本机数据代替。最终报告给出实测机器/节点数量/尺寸/采样窗口，不声称无限规模已验证。

## 已实测的实施结果

固定4槽位已取消。M5/macOS/硬件Metal renderer，真实Electron画布8/16/32张卡各30秒：120/120/119.9 FPS，帧间隔P95 9.3/9.2/10.1ms，均0长任务；所有可见节点均有动画。四节点结束、离屏、全卸载均验证。每档仅一个样本，不外推低配设备/长期耗电。

旧实现先红：八节点expected8、实际4；静态格子断言收到旧蓝条。修复后27项局部测试通过（后将一个浏览器测试移出unit，保持CI unit无Chromium依赖）；process-feedback视觉27态全部通过，无基线更新。真实队列走供应商loopback，真实图片预览、最终图揭示、zh/en、鼠标平移、动态减弱切换通过；最终遮罩1124.5ms移除。费用0，不冒称真实付费供应商试跑。详细逐改审计与原始证据：`docs/audit/2026-09-20-waiting-grid-review.md`。


## 全量验收发现的连带修复

- 磁吸把手测试把168上限写成固定高度；现役图片比例正确使240宽卡片高135，因此应得163。仅修测试，新增320宽/180高卡片覆盖168上限；原拖线、跟随和点击验收继续保留。红绿证据与逐改理由见总审计。
- Agent更新既有镜头仍请求整画布fit，导致80%阅读视口降为compact。真实旧构建加稳定窗口后可确定性复现；修在原批量落地共享边界，不在测试里重新放大。独立方案和根因合同随附。


## 性能脚本导航收敛

首轮远端21场景串行跑到33.5分钟上限，在media-error准备阶段被终止。18个已完成sample多数elapsed约50秒、交互采样仅2–7秒；冷打开19秒和多选拖动22秒。Linux是SwiftShader，等待效果实际为8个静态格子，因此这次失败不能归因于32张GPU格子。所有已有预算、warmup、采样数量与场景清单保持。

`ProjectLibraryPage.tsx:505`卡片点击与`:552`继续创作是同一打开操作；性能脚本旧openProject在单击后盲等1秒，再尝试旧卡片的继续创作并吞错。导航中目标窗口尚未登记时可能在旧窗口等待30秒。本机计时未复现第二点击等待（count=0，blank-pan21.1秒），因此不把推断写成已证明根因。

删除冗余打开和吞错，复用原getTargetWindow与现有expect.poll等待指定fixture项目URL，之后仍执行全部舞台/媒体稳定断言。样本记录setupMs/openMs用于远端定位；这不是放宽超时或性能预算。机械数门确认getTargetWindow仅benchmark内一扇读门，另核对cold-open/reload-heavy的已有导航链路。测试辅助逻辑变更，无生产代码/新引擎，无新增v3生产合同。验证单次打开的cold-open、blank-pan、waiting-effects，再跑原Linux全套；未完成前不能声称性能CI绿。
